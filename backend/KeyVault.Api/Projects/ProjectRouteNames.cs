namespace KeyVault.Api.Projects;

internal static class ProjectRouteNames
{
	internal const string GetProject = nameof(GetProject);
	internal const string GetEnvironments = nameof(GetEnvironments);
}
